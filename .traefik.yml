displayName: Cloudflare Gate
type: middleware
iconPath: .assets/icon.png

import: github.com/sstoner/cloudflaregate

summary: 'Middleware to verify requests originate from Cloudflare IPs'

testData:
  strictMode: true
  refreshInterval: "24h"
  allowedIPs:
    - "192.168.1.0/24"